
1.

Select Doctor_Name,Patient_Name,max(Total_bill) from Hospital group by Doctor_Name,Patient_Name;

2.

select Doctor_Name,Hospital_Name from Hospital group by hosp_name having count(hosp_name)>1;


3.

select Patient_Name from Hospital where Room_No=203 AND (discharge_date-admission_date)<=5;

4.

select Patient_Name,total_bill from Hospital where exists( select Hosp_Id,count(*) from Hospital group by Hosp_Id having count(*)>2) group by hosp_name;

5.

select count(distinct(patient_name) from hospital group by hosp_name;

6.

select hosp_name,room_no,max(total_bill) from hospital group by hosp_name,room_no;